import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    inputsView: {
        flexDirection: 'row',
    },


});

export default styles;